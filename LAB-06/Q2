#include <stdio.h>
int main () {
int num, p, i;
i=1;
printf("Enter your num");
scanf("%d", &num);
for (;(num<10 && num>=0)|| (num>-10 && num<0);) {
printf("Your number doesn not have multiple digits.");
return 0;
}
printf("Your number has multiple digits.");
return 0;
}
