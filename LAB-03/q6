#include <stdio.h>
int main() {
    float x1,y1,x2,y2, slope;
	x1=5;
	x2=3;
	y2=2;
	y1=4;
	slope=(y2-y1)/(x2-x1);
	printf("the slope of the line passing through (5,4) and (3,2) is: %.3f\n", slope);
	return 0;
	}
